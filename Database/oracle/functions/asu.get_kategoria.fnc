DROP FUNCTION ASU.GET_KATEGORIA
/

--
-- GET_KATEGORIA  (Function) 
--
--  Dependencies: 
--   STANDARD (Package)
--   SYS_STUB_FOR_PURITY_ANALYSIS (Package)
--   TKATEGORIA (Table)
--
CREATE OR REPLACE FUNCTION ASU."GET_KATEGORIA" 
  ( pFK_ID IN NUMBER)
  RETURN  VARCHAR2 IS
  CURSOR cTemp IS SELECT FC_NAME FROM TKATEGORIA WHERE FK_ID=pFK_ID;
  strTemp VARCHAR2(100);
BEGIN
  OPEN cTemp;
  FETCH cTemp INTO strTemp;
  CLOSE cTemp;
  RETURN strTemp;
END;
/

SHOW ERRORS;


