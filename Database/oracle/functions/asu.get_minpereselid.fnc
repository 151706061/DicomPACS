DROP FUNCTION ASU.GET_MINPERESELID
/

--
-- GET_MINPERESELID  (Function) 
--
--  Dependencies: 
--   STANDARD (Package)
--   SYS_STUB_FOR_PURITY_ANALYSIS (Package)
--   TPERESEL (Table)
--
CREATE OR REPLACE FUNCTION ASU."GET_MINPERESELID" 
  ( pFK_ID IN NUMBER)
  RETURN  NUMBER IS
  CURSOR cTemp IS SELECT /*+first_row*/ MIN(FK_ID) FROM TPERESEL WHERE FK_PACID=pFK_ID;
  nTemp NUMBER;
BEGIN
  OPEN cTemp;
  FETCH cTemp INTO nTemp;
  CLOSE cTemp;
  RETURN nTemp;
END;
/

SHOW ERRORS;


