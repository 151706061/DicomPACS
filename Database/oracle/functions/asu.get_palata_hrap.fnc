DROP FUNCTION ASU.GET_PALATA_HRAP
/

--
-- GET_PALATA_HRAP  (Function) 
--
--  Dependencies: 
--   STANDARD (Package)
--   SYS_STUB_FOR_PURITY_ANALYSIS (Package)
--   TPERESEL (Table)
--   GET_PACHRAP (Function)
--   TKARTA (Table)
--
CREATE OR REPLACE FUNCTION ASU."GET_PALATA_HRAP" 
  ( pFK_ID IN NUMBER,pFD_DATA IN DATE)
  RETURN  VARCHAR2 IS
  CURSOR cTemp IS SELECT /*+rule*/GET_PACHRAP(TKARTA.FK_ID) FROM TKARTA,TPERESEL where
    FD_DATA1<=pFD_DATA AND FD_DATA2>=pFD_DATA AND TPERESEL.FK_PALATAID=pFK_ID AND TPERESEL.FK_PACID=TKARTA.FK_ID;
  nTemp NUMBER;
BEGIN
  OPEN cTemp;
  LOOP
    FETCH cTemp INTO nTemp;
    EXIT WHEN cTemp%NOTFOUND;
    IF nTemp>0 THEN
      RETURN 'Храпят';
    END IF;
  END LOOP;
  RETURN '';
END;
/

SHOW ERRORS;


