DROP FUNCTION ASU.GET_TIB_ISPARAM
/

--
-- GET_TIB_ISPARAM  (Function) 
--
--  Dependencies: 
--   STANDARD (Package)
--   SYS_STUB_FOR_PURITY_ANALYSIS (Package)
--   TIB (Table)
--
CREATE OR REPLACE FUNCTION ASU."GET_TIB_ISPARAM" ( pPID IN NUMBER, pSMID IN NUMBER)
  RETURN  NUMBER IS
  nTemp NUMBER;
  CURSOR cTemp IS SELECT FK_SMID FROM TIB WHERE FK_PACID=pPID and FK_SMID=pSMID;
BEGIN
  OPEN cTemp;
  FETCH cTemp INTO nTemp;
  CLOSE cTemp;
  RETURN nTemp;
END;
/

SHOW ERRORS;


