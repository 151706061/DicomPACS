DROP TABLE ASU.TPROFFACTOR_1Q CASCADE CONSTRAINTS
/

--
-- TPROFFACTOR_1Q  (Table) 
--
CREATE TABLE ASU.TPROFFACTOR_1Q
(
  FK_ID      INTEGER                            NOT NULL,
  FK_PARENT  INTEGER,
  CODE       VARCHAR2(10 BYTE),
  NAME       VARCHAR2(500 BYTE)
)
TABLESPACE USR
PCTUSED    0
PCTFREE    10
INITRANS   1
MAXTRANS   255
STORAGE    (
            INITIAL          64K
            NEXT             1M
            MINEXTENTS       1
            MAXEXTENTS       UNLIMITED
            PCTINCREASE      0
            BUFFER_POOL      DEFAULT
           )
NOLOGGING 
NOCOMPRESS 
NOCACHE
NOPARALLEL
MONITORING
/


--
-- TPROFFACTOR1Q_BEFORE_INSERT  (Trigger) 
--
--  Dependencies: 
--   TPROFFACTOR_1Q (Table)
--
CREATE OR REPLACE TRIGGER ASU.TPROFFACTOR1q_BEFORE_INSERT
 BEFORE 
 INSERT
 ON ASU.TPROFFACTOR_1Q  REFERENCING OLD AS OLD NEW AS NEW
 FOR EACH ROW
BEGIN
  IF (USERENV('CLIENT_INFO') is null) or (USERENV('CLIENT_INFO') <> '%MAIL%') THEN
    BEGIN
    -- ORIGINAL TRIGGER BODY BEGIN FROM HERE:

BEGIN
  IF :NEW.fk_id IS NULL
  THEN
    SELECT seq_proffactor.NEXTVAL
      INTO :NEW.fk_id
      FROM DUAL;
  END IF;
END;
    -- ORIGINAL TRIGGER BODY ENDS HERE
    END;
  END IF;
END;
/
SHOW ERRORS;


