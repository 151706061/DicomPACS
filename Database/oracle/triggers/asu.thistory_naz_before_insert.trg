DROP TRIGGER ASU.THISTORY_NAZ_BEFORE_INSERT
/

--
-- THISTORY_NAZ_BEFORE_INSERT  (Trigger) 
--
--  Dependencies: 
--   STANDARD (Package)
--   DUAL (Synonym)
--   SEQ_THISTORY_NAZ (Sequence)
--   THISTORY_NAZ (Table)
--
CREATE OR REPLACE TRIGGER ASU."THISTORY_NAZ_BEFORE_INSERT" 
 BEFORE
  INSERT
 ON asu.thistory_naz
REFERENCING NEW AS NEW OLD AS OLD
 FOR EACH ROW
BEGIN
  SELECT SEQ_THISTORY_NAZ.NEXTVAL INTO :NEW.FK_ID FROM DUAL;
END;
/
SHOW ERRORS;


