DROP TRIGGER ASU.TIS_DISP_POPULDATA_IND_STA2_BI
/

--
-- TIS_DISP_POPULDATA_IND_STA2_BI  (Trigger) 
--
--  Dependencies: 
--   STANDARD (Package)
--   DUAL (Synonym)
--   SEQ_TIS_DISP_POPULDATA_IND_ST2 (Sequence)
--   TIS_DISP_POPULDATA_IND_STAGE2 (Table)
--
CREATE OR REPLACE TRIGGER ASU.TIS_DISP_POPULDATA_IND_STA2_BI
  BEFORE INSERT
  ON ASU.TIS_DISP_POPULDATA_IND_STAGE2   REFERENCING OLD AS OLD NEW AS NEW
  FOR EACH ROW
BEGIN
  IF :NEW.fk_id IS NULL
  THEN
    SELECT asu.seq_TIS_DISP_POPULDATA_IND_ST2.NEXTVAL
      INTO :NEW.fk_id
      FROM DUAL;
  END IF;
END;
/
SHOW ERRORS;


